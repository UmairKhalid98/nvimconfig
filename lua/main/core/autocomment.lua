function commentToggle(int x)
    -- get the file type
    -- print it out 
    -- if passed command is 1, comment the paragraph
    -- otherwise comment standard ie // or -- whatever the standard is 

end
